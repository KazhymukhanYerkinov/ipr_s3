name: file_secure_inspector
description: DevTools extension for File Secure App — Security & Command inspector.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.3.0
  devtools_app_shared: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
